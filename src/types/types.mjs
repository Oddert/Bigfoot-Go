/**
 * @typedef {Object} BigfootDatum A data-point representing a sighting instance.
 * @property {number} number The "official"* reference number of the sighting.
 * @property {string} title A brief description of the sighting report.
 * @property {string} classification The sighting classification (idk, don't ask).
 * @property {string} timestamp UTC timestamp of the sighting.
 * @property {number} latitude The coordinate latitude.
 * @property {number} longitude The coordinate longitude.
 */

// * I'm not sure what constitutes an official sighting.